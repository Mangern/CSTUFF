magic: () -> *int = {
    return alloc(int, 1 + 2 + 3);
}

main: () -> void = {
    one: *int = alloc(int);
    one.* = 69;
    // TODO: defer delete(one);

    x: *int = magic();
    // TODO: defer delete(x);

    i := 0;
    while (i < 6) {
      // (x + i * sizeof(int)).* = i;
      x[i] = i;
      i += 1;
    }
    println(one.*); // non-deterministic test incoming?
    println(x[0], x[5]);
    //delete(x);
    //delete(one);
}
