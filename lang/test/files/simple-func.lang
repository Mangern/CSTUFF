global_var: int;

sum: (x: int, y: int) -> int = {
    global_var = global_var + 1;
    return x + y;
}

main: () -> void = {
    x: int = 34;
    y: int = 35;
    println(sum(x, y));
    println(global_var);
}
