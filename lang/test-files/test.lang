int x := 5;
real y;
int z := 6;
int global := 10;

int[10][11] arr;

mul1 := (int x) -> int {
    return x * z;
}

mul := (int x, int y) -> int {
    int y;
    {
        int y;
    }
    return x * y;
}

increment := (int x) -> int {
    return 1 + x;
}

subs := (int x) -> int {
    return 2 - 3 - 4 - 5;
}

prec := (int x) -> int {
    return 2 - 3 * 4 - 5 + x * 2;
}

prec2 := () -> int {
    return 2 - 3 * (1 + 2 - 3);
}

negate := (int x := 3) -> int {
    int y;
    {
        string z := "foo";
        print(z);
    }
    return x + -x + prec2(2, 3 + 3, x - 4 - 1);
}

test := (int x) -> int {
    return 3 + test(3);
}

countChars := (string s) -> int {
}

main := () -> void {
    print(global);
    global := cast(char, 123.0);
    print(global);
}
